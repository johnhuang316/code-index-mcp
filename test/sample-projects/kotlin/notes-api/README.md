# Notes API (Kotlin)

Small Kotlin sample to exercise tree-sitter Kotlin parsing in Code Index MCP.

## Structure
```
src/main/kotlin/com/example/notes/
├── Main.kt
├── NotesApp.kt
├── NotesService.kt
├── Models.kt
└── Repositories.kt
```

Features: simple CRUD-style service, interface + implementation, function calls across classes.